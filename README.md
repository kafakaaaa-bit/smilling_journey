# Smiling Journey

Frontend-only React + Vite + TypeScript website for Smiling Journey.

## Run

```bash
npm install
npm run dev
```

## Verify

```bash
npm run lint
npm run typecheck
npm run build
```

The planner and contact form currently simulate submission. Backend, email, WhatsApp Business API, authentication, payments, and deployment are intentionally not implemented in this phase.
